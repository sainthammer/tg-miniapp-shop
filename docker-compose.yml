services:
  telegram-shop:
    build: .
    container_name: telegram-shop
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - shop_data:/app/data
      - ./static/uploads:/app/static/uploads

volumes:
  shop_data:
